# Copy to .dev.vars (for wrangler/next dev) – never commit real values.
PAYREXX_INSTANCE=velora
PAYREXX_API_KEY=
RESEND_API_KEY=
SITE_URL=http://localhost:3000   # for `pnpm cf:preview`/wrangler dev use the wrangler port, e.g. http://localhost:8787
